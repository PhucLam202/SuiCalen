# Sui Autopay Configuration

# Package and Registry IDs (deployed contract addresses)
VITE_AUTOPAY_PACKAGE_ID=0x...
VITE_REGISTRY_ID=0x...

# Relayer Configuration
# Base64 encoded 32-byte secret key for the relayer wallet
# The relayer signs transactions but does NOT pay gas (Gas Station pays)
RELAYER_PRIVATE_KEY=

# Gas Station Configuration (Sponsored Transactions)
# Base64 encoded 32-byte secret key for the Gas Station wallet (Admin)
# This wallet pays gas fees for all relayer transactions
GAS_STATION_PRIVATE_KEY=

# Maximum gas budget per transaction in MIST (1 SUI = 1,000,000,000 MIST)
# Default: 20000000 (0.02 SUI) - capped to prevent drain attacks
GAS_BUDGET_LIMIT=20000000
